rules_version = '2';
// The Azure Functions Admin SDK bypasses these rules. Lock the client SDK out
// so a leaked web config cannot write the guestbook from the browser.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
